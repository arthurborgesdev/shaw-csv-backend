{
  "name": "shaw-csv-backend",
  "version": "0.1.0",
  "description": "REST API in Node to load and query CSV",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "csv",
    "api",
    "query"
  ],
  "author": "Arthur Borges",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
